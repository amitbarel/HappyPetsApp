<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.HappyPets" parent="Theme.Material3.Light.NoActionBar">
        <item name="colorPrimary">@color/app_bg_color</item>
        <item name="radioButtonStyle">@style/CustomRadioButton</item>

    </style>

    <style name="CustomTextInputLayout" parent="Widget.MaterialComponents.TextInputLayout.OutlinedBox">
        <item name="boxBackgroundColor">@color/white</item>
        <item name="boxStrokeColor">@color/royal_blue</item>
        <item name="hintTextColor">@color/royal_blue</item>
        <item name="helperTextTextColor">@color/royal_blue</item>
        <item name="errorTextColor">#FF0000</item>
        <item name="counterTextColor">@color/black</item>
        <item name="placeholderTextColor">@color/black</item>
    </style>

    <!-- Custom style for TextInputEditText -->
    <style name="CustomTextInputEditText" parent="Widget.MaterialComponents.TextInputEditText.OutlinedBox">
        <item name="android:textColor">@color/black</item>
        <item name="android:textColorHint">@color/black</item> <!-- Use textColorHint instead of hint -->
    </style>

    <style name="Theme.HappyPets" parent="Base.Theme.HappyPets" />

    <style name="CustomRadioButton" parent="Widget.AppCompat.CompoundButton.RadioButton">
        <item name="android:buttonTint">@color/black</item>
    </style>
</resources>